# Окно последних упоминаний для проверки всплеска негатива.
NEGATIVE_WINDOW=10
# Доля негатива в окне, при которой поднимается алерт.
NEGATIVE_THRESHOLD=0.5
# Хост и порт API.
APP_HOST=0.0.0.0
APP_PORT=8000
